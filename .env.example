VITE_USE_MOCK_API=true
VITE_API_URL=http://localhost:5000/api/v1
VITE_APP_NAME=CubixGear Company Panel
VITE_APP_VERSION=1.0.0

# Never put API secrets, storage secret keys, payment gateway secrets,
# database credentials or private tokens in VITE_* variables.
# VITE_* values are bundled into the browser application.
