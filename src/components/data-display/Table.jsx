import React from 'react';
export const Table = ({ columns = [], data = [], renderRow, className = '' }) => <div className={`ui-table-wrap ${className}`.trim()}><table className="ui-table"><thead><tr>{columns.map((column) => <th key={column.key || column.label}>{column.label}</th>)}</tr></thead><tbody>{data.length ? data.map((row, index) => <tr key={row.id || index}>{renderRow ? renderRow(row, index) : columns.map((column) => <td key={column.key}>{row[column.key]}</td>)}</tr>) : <tr><td colSpan={Math.max(columns.length, 1)} style={{ textAlign: 'center' }}>No records found.</td></tr>}</tbody></table></div>;
