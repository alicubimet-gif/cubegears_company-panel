import React, { useEffect } from 'react';
import { X } from 'lucide-react';
import { IconButton } from '../ui/IconButton';
export const Modal = ({ isOpen, open, onClose, title, children, footer, className = '' }) => { const visible = open ?? isOpen ?? true; useEffect(() => { if (!visible) return undefined; const close = (event) => event.key === 'Escape' && onClose?.(); document.addEventListener('keydown', close); return () => document.removeEventListener('keydown', close); }, [onClose, visible]); if (!visible) return null; return <div className="ui-overlay" role="presentation" onMouseDown={(event) => event.target === event.currentTarget && onClose?.()}><section className={`ui-modal ${className}`.trim()} role="dialog" aria-modal="true" aria-label={title}><header className="ui-modal__header"><h3 className="ui-card__title">{title}</h3><IconButton label="Close" onClick={onClose}><X size={18} /></IconButton></header><div className="ui-modal__body">{children}</div>{footer && <footer className="ui-modal__footer">{footer}</footer>}</section></div>; };
