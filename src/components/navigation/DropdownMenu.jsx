import React, { useEffect, useRef, useState } from 'react';
export const DropdownMenu = ({ trigger, items = [], className = '' }) => { const [open, setOpen] = useState(false); const ref = useRef(null); useEffect(() => { const close = (event) => !ref.current?.contains(event.target) && setOpen(false); document.addEventListener('mousedown', close); return () => document.removeEventListener('mousedown', close); }, []); return <div className={`ui-dropdown ${className}`.trim()} ref={ref}><span onClick={() => setOpen((value) => !value)}>{trigger}</span>{open && <div className="ui-dropdown__menu" role="menu">{items.map((item) => <button key={item.label} type="button" className="ui-dropdown__item" onClick={() => { item.onClick?.(); setOpen(false); }}>{item.label}</button>)}</div>}</div>; };
